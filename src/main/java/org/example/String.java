//package org.example;

public class String {/*
    .startsWith() 문자열로 시작하는가?
            .endsWith() 문자열로 끝나는가?
            .replace() 문자열 변환
    .substring() 문자열 자르기 -> (7) 이면 7번째 문자열부터 출력
    .trim() 앞뒤 공백 제거
    .concat() 문자열 결합*/

    /*
    ArrayList
    add(E e)	요소 추가 (맨 끝에)
    add(int index, E e)	특정 위치에 요소 추가
    get(int index)	특정 인덱스의 요소 반환
    set(int index, E e)	특정 인덱스의 요소 수정
    remove(int index)	인덱스로 요소 삭제
    remove(Object o)	객체로 요소 삭제 (처음 나타나는 것 하나만)
    clear()	모든 요소 삭제
    size()	요소 개수 반환
    isEmpty()	비어있는지 확인 (true/false)
    contains(Object o)	특정 요소가 있는지 확인
    indexOf(Object o)	요소의 인덱스 반환 (없으면 -1)
    toArray()	배열로 변환
    forEach(Consumer<? super E> action)	람다식 등으로 모든 요소 처리
*/
    /*
    메서드	설명
length()	문자열의 길이를 반환
charAt(int index)	특정 인덱스의 문자 반환
substring(int beginIndex)	beginIndex부터 끝까지 자른 문자열 반환
substring(int beginIndex, int endIndex)	beginIndex부터 endIndex 전까지 자름
equals(Object obj)	문자열이 같은지 비교 (== 말고 이걸 써야 함)
equalsIgnoreCase(String another)	대소문자 무시하고 비교
contains(CharSequence s)	특정 문자열이 포함되어 있는지 확인
startsWith(String prefix)	특정 문자열로 시작하는지 확인
endsWith(String suffix)	특정 문자열로 끝나는지 확인
indexOf(String str)	특정 문자열의 처음 위치 반환 (없으면 -1)
lastIndexOf(String str)	마지막으로 등장한 위치 반환
toUpperCase()	대문자로 변환
toLowerCase()	소문자로 변환
trim()	앞뒤 공백 제거
replace(CharSequence target, CharSequence replacement)	특정 문자열을 다른 문자열로 변경
split(String regex)	구분자를 기준으로 문자열을 배열로 분리
concat(String str)	문자열 이어 붙이기 (하지만 + 연산자가 더 자주 쓰임)
     */

}